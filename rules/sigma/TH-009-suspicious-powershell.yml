title: Suspicious PowerShell Execution Pattern
id: 2bb7f009-43d0-4a09-8c09-7d6a1f090009
status: experimental
description: |
  Detects PowerShell command lines that combine adversary-typical behaviours - base64 encoding,
  hidden non-interactive execution, download cradles, in-memory execution, execution-policy bypass -
  or that tamper with local defences, or that are launched by a parent which has no legitimate
  reason to start a shell. Encoded payload content is not visible here; enable script block logging
  (4104) for that.
references:
  - https://attack.mitre.org/techniques/T1059/001/
  - https://attack.mitre.org/detectionstrategies/DET0455
author: Hamza Khella
date: 2026-08-31
modified: 2026-09-10
tags:
  - attack.execution
  - attack.t1059.001
logsource:
  category: process_creation
  product: windows
detection:
  selection_powershell:
    Image|endswith:
      - '\powershell.exe'
      - '\pwsh.exe'
  sig_encoding:
    CommandLine|re: '\s-{1,2}e(nc|ncodedcommand)?\s+[A-Za-z0-9+/=]{20,}'
  sig_cradle:
    CommandLine|contains:
      - 'DownloadString'
      - 'DownloadFile'
      - 'Invoke-WebRequest'
      - 'Invoke-RestMethod'
      - 'Net.WebClient'
      - 'MSXML2.XMLHTTP'
      - 'Start-BitsTransfer'
  sig_in_memory:
    CommandLine|contains:
      - 'IEX'
      - 'Invoke-Expression'
      - 'Reflection.Assembly'
      - '[scriptblock]::Create'
      - 'FromBase64String'
  sig_hidden:
    CommandLine|contains:
      - '-w hidden'
      - '-WindowStyle Hidden'
      - '-NonInteractive'
      - '-ExecutionPolicy Bypass'
      - '-ep bypass'
  high_defence_change:
    CommandLine|contains:
      - 'Set-MpPreference'
      - 'Add-MpPreference'
      - 'DisableRealtimeMonitoring'
      - 'ExclusionPath'
      - 'AmsiUtils'
      - 'amsiInitFailed'
      - 'Clear-EventLog'
  parent_suspicious:
    ParentImage|endswith:
      - '\winword.exe'
      - '\excel.exe'
      - '\powerpnt.exe'
      - '\outlook.exe'
      - '\mshta.exe'
      - '\wscript.exe'
      - '\cscript.exe'
      - '\w3wp.exe'
  condition: selection_powershell and (high_defence_change or 2 of sig_* or (parent_suspicious and 1 of sig_*))
fields:
  - CommandLine
  - ParentImage
  - User
falsepositives:
  - Software deployment and configuration management that uses encoded commands for quoting.
  - Monitoring agents and installers running scripted PowerShell from a fixed path.
  - Administrators using -NoProfile -ExecutionPolicy Bypass in documented workflows.
level: high
